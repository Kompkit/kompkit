name: kompkit_core
description: Cross-platform utility functions for Flutter and Dart applications. Part of the KompKit ecosystem.
version: 0.1.0-alpha
homepage: https://github.com/Kompkit/KompKit
repository: https://github.com/Kompkit/KompKit

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.20.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  test: ^1.25.0

flutter:
  # This package does not contain any Flutter-specific functionality
  # but is compatible with Flutter applications
